# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/
# Web frontend vendor: pinned + SRI-hashed third-party libs we self-host
# instead of loading from a CDN. Tracked in git so we control the bytes
# the browser actually executes.
!internal/web/static/js/vendor/
!internal/web/static/js/vendor/**

# Go workspace file
go.work
go.work.sum

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Local configuration files
.claude/settings.local.json
.claude/agents/
# Local-only agent skill tooling (release-manager); workspace-planning stays tracked
.agents/skills/release-manager/
.env
.env.local

# Runtime/cache directories
plugin_cache/
uploaded_plugins/
test_plugins/
local_plugin_registry.json
plugin_registry_cache.json
marketplace_cache_*.json
cache/
tmp/
temp/
.gocache/
.gocache-build/
.gobuild/

# Agent data and configuration
agents/*
!agents/skills/
!agents/skills/**
agents.json


# Build artifacts
dist/
build/
!build/macos/
!build/linux/
!build/windows/
build-dmg/
release/
releases/

# Log files
*.log
logs/
activity_logs/

# Database files
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
model_categories.json

# Backup files
*.bak
*.backup
*.backup-*
*~
.backup-*/
backup-*/

# Local development files
.local/
local.json
dev.json

# Node.js (if any frontend tooling is added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python (if any Python scripts are added)
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# Temporary files from various tools
.tmp/
*.tmp
*.tempfrontend/dist/

# Runtime data directories
agents.json

workspaces/
!internal/web/templates/components/workspaces/
!internal/web/templates/components/workspaces/**

ori-agent
/server
settings.json
!.claude/settings.json
app_state.json
location.json
mcp_registry.json
mcp_servers.json
mcp_search_cache.json
workspace_allowlist.json

# Agent-specific runtime files
agents/*/agent_settings.json
agents/*/workspace/
internal/server/agents/
internal/pluginhttp/

# Session data
.claude-sessions/
sessions/
*.session

tasks/
marketplace_config.json
.gomodcache/
session_files/
tests/e2e/ori-data/

# Runtime app data under ori-data/
# Keep the live app profile out of Git, but allow the checked-in seed agent configs.
ori-data/**
!ori-data/agents/
!ori-data/agents/Ori/
!ori-data/agents/Ori/**
!ori-data/agents/default/
!ori-data/agents/default/**
ori-data/agents/*/agent_settings.json

# User uploads and custom templates
uploads/
workflow_templates/

# Misplaced generated protobuf files
github.com/


# Runtime project-templates library (materialized on startup; user data)
/templates/
# Test-run artifact: server builder tests materialize starters into the package dir
internal/server/templates/
