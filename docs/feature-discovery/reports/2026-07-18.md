# Feature Discovery — 2026-07-18 (first run)

## Shortlist

### 1. MCP 2026-07-28 spec compliance  [Impact: H | Effort: M | Fit: H]
- What: Update `internal/mcp/` for the largest MCP revision since launch, final **July 28, 2026** (10 days): stateless Streamable HTTP (Mcp-Session-Id removed), per-connection list variance gone, extensions framework (Tasks for long-running work, MCP Apps for server-rendered UI), tightened OAuth (RFC 9207 iss validation), formal deprecation policy.
- Why now (C, A): RC published — https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/ ; MCP is a core pillar (`internal/mcp/`, per-workspace bindings). The Tasks extension maps naturally onto Workspace Runs; MCP Apps could render tool UIs in workspace chat.
- First PRD questions: Support dual spec revisions or hard floor? Adopt Tasks extension as the Workspace Runs transport? What breaks in existing per-workspace bindings?

### 2. Action Center periodic delivery wiring  [Impact: H | Effort: S | Fit: H]
- What: Wire the already-built follow-up staleness evaluation + nudge idempotency into a periodic scheduler, and let workspace findings (incl. the future discovery shortlist) actually arrive.
- Why now (D, E): Explicitly deferred from Personal HQ v1 with "only the periodic wiring is pending" (`docs/features/personal-hq-assistant.md`); unblocks dogfood stage 3 (backlog entry); `internal/personalhq*` is the hottest area right now (mtime scan).
- First PRD questions: One global scheduler or per-workspace? Delivery cadence/quiet hours? Does stage-3 shortlist delivery ride the same pipe as follow-up nudges?

### 3. `backlog` skill in Ori  [Impact: M | Effort: S | Fit: H]  *(backlog-originated)*
- What: Skill in `internal/skills/` with add/list/prune/promote on a workspace `BACKLOG.md`; bind to the Ori DevOps workspace. Dogfood stage 1.
- Why now (E): Backlog entry tagged `#small`; smallest first exercise of skill authoring ergonomics; prerequisite habit for stages 2–3.
- First PRD questions: Skill-only or plus a workspace tool? Conventions enforced or advisory? Where does the skill store per-workspace config?

### 4. Skills marketplace parity: install-from-URL + version pinning  [Impact: M | Effort: M | Fit: H]
- What: One-click install from a marketplace/repo URL, version pinning, update checks in `skillshttp`.
- Why now (C, E): Claude Code ecosystem now has multi-thousand-skill marketplaces with pinning/updates/CLI package managers (e.g. https://github.com/jeremylongshore/claude-code-plugins-plus-skills , https://claudemarketplaces.com/ ) — becoming table stakes for skills platforms. Matching backlog idea already existed (`!` install-from-URL).
- First PRD questions: Trust/signing model for third-party skills? Pin by tag or hash? Compatibility with Ori's skill format vs. importing Claude-format skills?

### 5. CLI agent execution: replace the stub  [Impact: M | Effort: M | Fit: M]
- What: `internal/cliagenthttp/handlers.go:104` — "For now we return a placeholder. A production implementation would use channels." Finish streaming execution; `internal/cliagent` is 90d+ stale.
- Why now (A, B): Only true stub found in the codebase; it's the dependency for dogfood stage 4's build handoff (Ori → CLI coding agent).
- First PRD questions: Which CLI agents first-class (Claude Code?)? Streaming protocol to UI? How do runs appear in Workspace Runs?

## Watchlist
- Progress-tracking plan (`PROGRESS_TRACKING_PLAN.md`): 0 impl files — large, not started; revisit after stage 3.
- Task-input templating: 0 impl files; plan exists.
- `internal/chathttp` staleness: 42 files 90d+ old next to hottest code — refactor chore, not feature.
- Sandboxed agent execution (OpenAI Agents SDK direction: sandbox + workspace manifests) — watch; Workspace Runs partially covers.
- Daily Brief pulling DevOps shortlist; HQ management panel; journal→memory promotion (Personal HQ v1.x deferred list).

## Changes since last report
First run. `HQ cross-workspace pull access` already promoted to Doing (PRD: `tasks/prd-hq-cross-workspace-visibility.md`).

## Source log
- A: stub/placeholder grep (1 true stub: cliagenthttp); premium_features.md skimmed.
- B: git unavailable in sandbox (worktree gitdir) → mtime. Hot: workspace, server, sessionhttp, dailybrief, personalhq*, projecttemplates. Stale: chathttp, session, location, cliagent, vault.
- C: 4 searches (timebox ok): MCP spec RC, OpenAI Agents SDK 2026, LangGraph/CrewAI 2026, Claude Code skills ecosystem.
- D: 13 feature docs classified via impl grep — shipped/partial: personal-hq (v1 + deferred list), workspace-runs (29 files), output-contracts (18), session-file-mgmt (8); not started: progress-tracking (0), task-input-templating (0).
- E: 4 Ideas entries scored; 1 Doing.
