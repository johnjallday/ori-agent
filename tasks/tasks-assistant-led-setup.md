# Implementation tasks: Assistant-led setup — File Janitor first

## Planning status and scope

- **Requirements:** `tasks/prd-assistant-led-setup.md` — FR-01–FR-38 and AC-01–AC-20.
- **Approved parent groups:** the user replied **Go** to the five groups below.
- **Implementation handoff:** after planning, the user explicitly requested implementation with Pi. The authorized Pi builder executes this checklist only in the isolated `assistant-led-setup` feature worktree; the PR approval gate remains in force.
- **Feature identity:** `assistant-led-setup`; no GitHub Issue is attached.
- **Planning checkout:** `/Users/jjdev/Projects/ori/worktrees/ori-agent-dev`.
- **Reviewed revision:** `38a9394445e2572a82f0d69f793e1219ce627a25` in `https://github.com/johnjallday/ori-agent.git`.
- **Implementation/validation status:** not started; no build, test, browser demonstration, or native-picker exercise has been performed for this feature. Every checkbox starts unchecked.

Scope remains File Janitor only, after the existing hire, with explicit **Set up for me**, separate folder and monitoring permissions, metadata-only first results, and the existing file-review surface. No HQ requirement, model calls, general setup-intent classifier, autonomous file changes, or universal setup framework.

## Relevant Files

### Requirements, contracts, and presentation

- `tasks/prd-assistant-led-setup.md` — product requirements, consent boundaries, acceptance scenarios, and success metrics.
- `docs/architecture/personal-assistant-foundation-contract.md` — relationship identity, pre-HQ behavior, mission semantics, and read-only Ori Guide boundary.
- `docs/architecture/host-setup-quests.md` and `docs/architecture/specialist-setup-journey-contract.md` — existing orchestration contracts to compare before selecting persistence.
- `docs/workspace-team-readiness.md` — reviewed staffing and the distinction between a configured team and operational readiness.
- `docs/architecture/assistant-led-setup-contract.md` — **new**, findings-first architecture/state/permission/recovery contract written by group 1 and updated to match implementation.
- `internal/web/templates/components/ori-guide.tmpl` — the shared Personal Assistant panel; selected to render the one setup-card partial on non-Home pages after Assistant status, while Home renders the same partial after its Today/HQ banner.
- `internal/web/templates/components/dashboard.tmpl` — Home mission/discovery surface.
- `internal/web/templates/layout/base.tmpl` — shared module loading; preserve hire/HQ walkthrough ordering.
- `internal/web/static/js/modules/personal-assistant-panel.js` and `.test.js` — actual hired identity, panel opening, focus, and ordinary Ask availability.
- `internal/web/static/js/modules/personal-assistant-home.js` and `.test.js` — Home/Today projections, launcher cues, and existing domain offers.
- `internal/web/static/js/modules/tidy-downloads-quest.js` and `.test.js` — legacy compatible mission entry; currently a focus-only creator walkthrough.
- `internal/web/static/js/modules/setup-wizard.js` and `.test.js` — canonical wizard interaction, automatic resolution, and manual fallback behavior.
- `internal/web/static/js/modules/file-janitor-console.js` and `.test.js` — existing domain-specific folder/automation controls and final review/settings/history.
- `internal/web/static/css/ori-guide.css`, `internal/web/static/css/file-janitor.css` — existing panel/domain visual conventions.
- `internal/web/static/js/modules/assistant-led-setup.js` and `.test.js` — **selected new** focused card controller and pure view-state tests over the capability-specific Personal Assistant API.
- `internal/web/templates/components/assistant-led-setup.tmpl` — **selected new** single card partial, conditionally rendered once per page in the shared drawer.
- `internal/web/static/js/modules/file-janitor-setup-controls.js` — **rejected as speculative**; add no second module unless implementation establishes two real explicit-workspace consumers.

### Canonical domain services and creation

- `internal/projecttemplates/starter/file-janitor/template.json` — owned blueprint, File Curator, metadata defaults, directory requirement, automation disclosure, and wizard declaration.
- `internal/workspacecapability/builtin_file_janitor.go` — compiled capability identity and canonical/legacy adapter aliases.
- `internal/sessionhttp/workspace_handler.go` — production workspace creation and `CreateFromTemplate`; the latter is not itself a reviewed/replay-safe creation contract.
- `internal/sessionhttp/template_agents.go`, `internal/sessionhttp/template_agent_review.go`, `internal/sessionhttp/workspace_team_readiness.go` — effective staffing plan, strict review, create/reuse checks, and canonical agent seeding.
- `internal/sessionhttp/workspace_template_tasks.go` — wizard-owned setup-help completion; ordinary starter tasks must not auto-run.
- `internal/personalassistant/service.go` — canonical relationship and identity projection; do not weaken ordinary work eligibility.
- `internal/setupjourney/store.go`, `internal/setupjourney/action_service.go`, `internal/setupjourney/service.go`, `internal/specialist/setup_journey.go` — existing run/review/operation receipts and closed step-kind contracts to assess in group 1.
- `internal/setupwizard/service.go`, `internal/setupwizardhttp/handler.go`, `internal/setupwizardhttp/routes.go` — domain-backed readiness, progress, and first-ready completion.
- `internal/workspace/http_handlers_launcher.go`, `cmd/folder-picker/app.go`, `cmd/folder-picker/main.go`, `scripts/build-folder-picker.sh` — repository-owned native selection/helper build; not an external plugin.
- `internal/filejanitor/service.go`, `internal/filejanitor/setup_adapter.go`, `internal/filejanitor/settings_service.go` — folder grant, `Filed` creation, automation approval, privacy, pause/relink/revoke.
- `internal/filejanitor/automation.go`, `internal/filejanitor/scan_service.go`, `internal/filejanitor/scan_store.go`, `internal/filejanitor/scanner.go`, `internal/filejanitor/classifier_provider.go` — watcher/daily scan, atomic scan state, metadata-only classification, and initial-scan receipt integration.
- `internal/filejanitorhttp/handler.go`, `internal/filejanitorhttp/batches.go`, `internal/filejanitorhttp/routes.go` — owner-scoped grants, scanning, and existing review controls.
- `internal/filejanitor/approval.go`, `internal/filejanitor/preview.go`, `internal/filejanitor/apply.go` — existing file-operation authorization; regression targets, not new setup powers.

### Wiring, lifecycle, and proposed additions

- `internal/server/builder.go`, `internal/server/builder_handlers.go`, `internal/server/builder_dailybrief.go`, `internal/server/builder_setup_journey.go`, `internal/server/routes.go`, `internal/server/server.go` — dependency order, route registration, runtime ownership, and shutdown seams.
- `internal/server/builder_progression.go`, `internal/progression/quests.go` — mission routing, ready evidence, and one-time completion/reward integration.
- `internal/database/migrations.go`, `internal/database/reset_inspection.go`, `internal/database/reset_apply.go` — durable schema and explicit reset-domain inventory; allocate migration numbers at implementation time.
- `internal/resetstate/work_gate.go`, `internal/server/reset_admission.go`, `internal/server/reset_lifecycle.go` — finite/background work admission, drain, and reset safety.
- `internal/assistantsetup/` — **selected new** narrow coordinator/store package; declaration-backed `setupjourney` and a generic graph/runtime are explicitly rejected.
- `internal/personalassistanthttp/assistant_setup.go` — **selected new** bounded HTTP adapter reusing the existing current-user boundary.
- `internal/server/assistant_setup.go` — **selected new** canonical-owner integration and wiring, not a second workspace/domain implementation.
- `internal/server/testdata/route_paths.txt`, `internal/server/testdata/route_table.golden` — update only for actual approved new route shapes.

### Tests and delivery evidence

- `internal/sessionhttp/template_agent_review_test.go`, `workspace_create_rollback_test.go`, `workspace_create_setup_requirements_test.go`, `workspace_team_readiness_test.go`, `template_agents_root_test.go` in the same package — reviewed creation, partial failure, current root-agent storage, and snapshots.
- `internal/filejanitor/setup_adapter_test.go`, `setup_race_test.go`, `scan_service_test.go`, `scan_store_test.go`, `privacy_test.go`, `agent_free_golden_path_test.go`, `reset_admission_test.go`, `removal_no_mutation_test.go` in the same package — authoritative permission, replay, privacy, model-free runtime, and removal contracts.
- `internal/filejanitorhttp/authorization_audit_test.go`, `internal/filejanitorhttp/path_leak_test.go` — authorization/redaction patterns.
- `internal/server/reset_lifecycle_test.go`, `internal/server/reset_writers_test.go`, `internal/server/assistant_progression_test.go`, `internal/server/routes_golden_test.go` — real wiring and lifecycle regressions.
- `tests/file-janitor.spec.ts`, `tests/file-janitor.a11y.spec.ts`, `tests/downloads-janitor.spec.ts`, `tests/blueprint-setup-wizards.spec.ts` — existing File Janitor/legacy/manual paths.
- `tests/workspace-team-readiness.spec.ts`, `tests/starter-missions.spec.ts`, `tests/personal-assistant-foundation.spec.ts`, `tests/personal-assistant-foundation.a11y.spec.ts` — shared creation, mission, identity, and accessibility regressions.
- `tests/assistant-led-setup.spec.ts`, `tests/assistant-led-setup.a11y.spec.ts` — **new**, end-to-end interaction and accessibility coverage; real-domain evidence distinguished from mocked browser cases.
- `tasks/screenshots/assistant-led-setup/` — **new during implementation**, sanitized screenshots of each actual demonstration.
- `tasks/test-guide-assistant-led-setup.md` — **new after implementation**, manual guide reflecting the delivered feature.

### Notes

- Paths marked proposed are not claims that files exist. Group 1 settles exact storage/module boundaries; update this inventory before dependent work.
- The PRD was inspected at `ba462199`; current source includes agent-storage changes (#521), reviewed deletion changes (#525), and independent Home changes (#527). Revalidate these shared seams rather than applying an old creation shortcut.
- `ScanNow` currently has no caller operation key, and a successful scan with no eligible files creates no batch. Candidate deduplication alone cannot prove that an onboarding scan succeeded before a lost response. The plan requires a bounded durable outcome, including zero-candidate success, without manufacturing empty batches.
- Existing setup-journey review/operation receipts are useful precedents, but their typed step/declaration contracts do not establish that File Janitor can safely reuse them unchanged.
- `docs/agents.md`, referenced by repository guidance, was absent in the inspected checkout. Follow the current `AGENTS.md` and canonical planning skill; recheck availability at implementation preflight.
- Use the repository's test entry points and disposable data. Do not touch the shared checkout's existing `.ori-reset/` or `agent_state/` changes.

## Instructions for Completing Tasks

Implementation agent: **Pi primary, explicitly selected by the user**. Launch with `--kind pi` and the integration's default model; no specific model or persistent default change was requested. Model chips below are advisory task-routing recommendations, not launch overrides or persistent repository configuration changes.

Check off each sub-task immediately after its implementation and relevant verification are complete. Follow `.agents/skills/task-planning/SKILL.md` for execution, demos, commits, and the PR gate. A separate handoff creates the isolated feature worktree with `wt start assistant-led-setup`; never implement in `ori-agent-dev`.

After that handoff, work is continuous between the already-approved planning gate and the final PR gate, except for a real blocker or new authorization boundary. Do not mark a task complete because an adjacent mock-backed screen looks correct. Record unavailable live evidence honestly and name what remains blocked.

## Execution Topology

- **Integration owner:** one primary implementation agent in the feature worktree; it owns the server, UI, tests, docs, and final integration.
- **Writing rule:** one active writer at a time. Shared files include creation/staffing handlers, database migrations/reset tables, server wiring/routes, progression, the Assistant panel, and the Janitor console. They are not safe concurrent edit lanes.
- **Delegation:** none planned. Prefer model switches in the primary context. A bounded read-only security/recovery review may return findings without becoming a second implementation owner.
- **Deep-model hotspots:** group 1's architecture findings, reviewed creation/partial ownership (2.4–2.5), monitoring consent races (3.4), initial-scan replay/empty outcomes (3.6), and lifecycle invalidation (4.5–4.7). Routine UI, ordinary tests, and wiring use the parent balanced model.
- **Parallel-safe work:** no parallel implementation lanes. Once contracts are frozen, findings-only acceptance/security review or independent test-result inspection can run in parallel; any edits return to the integration owner.
- **Dependencies/build order:** 1 → 2 → 3 → 4 → 5. Each product group includes its own backend, frontend, tests, and real demo. Group 2 already provides durable run ownership and safe stop behavior; group 4 completes recovery UX and the full existing-state/lifecycle matrix rather than retrofitting all safety at the end.
- **Validation cadence:** focused tests after each narrow slice; affected Go/JS suites and a real demo before each group commit; full Go, ratcheted lint, scoped security, JS, affected browser, and smoke gates once at delivery.
- **Delivery shape:** one feature branch/worktree and one final PR to `dev`, with a commit per group. These slices share files and are not independent release PRs. If the settled implementation proves too large for a reviewable PR, record that finding and follow the canonical epic guidance rather than silently splitting dependent changes directly into `dev`.
- **Demo isolation:** use `wt demo` with disposable app data, workspace root, and test inboxes. No real Downloads/Desktop, credentials, private filenames, real-data resets, installed-plugin edits, or production service restarts. Identify the actual picker binary/source used in each live result.

## Companion Dependencies

**Assessment: not required for the approved scope.** File Janitor's blueprint, compiled runtime, HTTP boundaries, UI, and native picker source are all in Ori. No local plugin, shared SDK, external provider, or separately versioned integration change is indicated by the inspected call paths.

- **Source identity:** `https://github.com/johnjallday/ori-agent.git`, observed at `/Users/jjdev/Projects/ori/worktrees/ori-agent-dev`, revision `38a9394445e2572a82f0d69f793e1219ce627a25`. The editable helper is `cmd/folder-picker/`; `scripts/build-folder-picker.sh` resolves that directory from its own repository root. Generated Wails bindings, a built `.app`/executable, and any installed helper are not alternate editable source trees.
- **Change scope:** Ori-owned proposal/coordinator/card, canonical creation adapters, and narrowly needed scan receipts/lifecycle integration. No plugin blueprint/schema/published pin is changed. A blueprint/schema bump is needed only if group 1 establishes an actual changed persisted contract, not merely because the presentation changes.
- **Owner and preparation:** same primary integration owner after the isolated feature handoff. No companion worktree, separate owner, installation, or release is prepared or required. The feature worktree is not created by this planning session.
- **Validation:** group 1 verifies the existing helper on a supported local platform with disposable state and records its identity. Group 3 validates it against the exact Ori candidate. A mocked picker proves UI/error behavior only. The build script may install Wails if absent; inspect tool availability and authorization rather than silently provisioning tooling during planning or treating an untested helper as verified.
- **Delivery:** one Ori PR; no companion release or pin follow-up. Local candidate validation and production distribution remain separate facts.
- **Reassessment boundary:** if live discovery reveals a required change outside this repository, mark the assessment unresolved, record its source/revision/guidance and owner/worktree/validation/delivery needs, and block only dependent tasks until a separately authorized companion handoff exists. Do not modify installed software as a shortcut. Unsupported picker platforms remain a bounded error, not a new platform project.

## Requirement and acceptance coverage

| PRD area | Primary implementation tasks | Acceptance scenarios |
| --- | --- | --- |
| Entry, hire/HQ eligibility, recommendation/deferral (FR-01–FR-06) | 2.1–2.3, 2.6–2.9, 4.1–4.4 | AC-01, AC-02, AC-10, AC-19, AC-20 |
| Concrete review, scoped approval, repeat-safe creation (FR-07–FR-11, FR-14) | 1.2–1.3, 2.3–2.5, 2.10 | AC-03, AC-04, AC-11, AC-16 |
| Existing targets and manual handoff (FR-12–FR-13) | 2.2, 4.1–4.3 | AC-10, AC-13, AC-14, AC-19 |
| Automatic progress and folder permissions (FR-15–FR-20) | 2.9, 3.1–3.3 | AC-03, AC-05, AC-06, AC-12 |
| Monitoring, first result, privacy, milestones, no file approvals (FR-21–FR-28) | 3.4–3.10 | AC-01, AC-03, AC-07, AC-08, AC-09, AC-16, AC-19 |
| Durable state, recovery, bounded retries, invalidation (FR-29–FR-34) | 2.4–2.5, 4.3–4.9 | AC-04, AC-13, AC-14, AC-15, AC-17, AC-20 |
| Shared card, accessibility, operational exits (FR-35–FR-38) | 2.7–2.9, 3.9, 4.4, 4.8, 5.2 | AC-18, AC-19, AC-20 |
| Full delivered outcome and measured claims | 5.1–5.11 | AC-01–AC-20 |

## Tasks

- [x] 1.0 Resolve integration and recovery risks in one findings-first spike `Model: Opus 5`
  - [x] 1.1 Revalidate the PRD, current feature-worktree revision, repository guidance, and Companion Dependencies before touching implementation. Inspect changes since the recorded baseline, especially root-agent storage, strict team creation, and reviewed workspace deletion. Record the actual base and relevant drift in the findings contract; do not reset or repair shared local state.
  - [x] 1.2 Trace one fresh and one existing File Janitor setup through relationship eligibility, hydrated workspace identity, creator/team review, wizard readiness, folder grant, monitoring, and scan storage. Compare reusing `setupjourney` with a narrow `assistantsetup` coordinator. Choose the least invasive design that preserves typed domain authority, pre-HQ access, non-creating reads, and consent; explicitly reject a generic graph/agent runtime. Name storage ownership, HTTP/module boundaries, and allowed operation/receipt fields.
  - [x] 1.3 Map crash windows for agent creation, workspace persistence/provisioning, folder setup, automation approval/registration, scan persistence, and final acknowledgment. Specify how stable operation ownership reaches each canonical resource before a lost response can orphan it; distinguish atomic domain writes from cross-store steps. Settle a replay proof for successful zero-candidate scans and a safe unresolved-outcome stop. Account for manual actor changes, root generation, changed privacy, workspace deletion, and reset before any automatic continuation is designed.
  - [x] 1.4 Exercise the existing manual path in an isolated demo with no model/HQ requirement for Janitor: native picker success/cancel/unavailable, paused folder grant and `Filed` creation, explicit automation, readiness, and metadata scan. Use only a disposable inbox. Record supported environment/helper identity, actual behavior, decision/page-transition counts, and baseline timings; mark unavailable live observations NOT RUN. Verify the helper build path uses the isolated checkout, not an installed plugin/source copy.
  - [x] 1.5 Validate the card's placement in the real shared panel on Home and another app page, including `needs_hq`, active, and paused identity states, narrow width, nested picker focus, and the HQ briefing. Add focused characterization tests for the current no-agent/no-model domain path, strict creation, and reset admission where needed to pin the findings. An optional transient UI sketch is a **Prototype demo**, not product evidence, and must not become an enabled mock-backed feature.
  - [x] 1.6 Write `docs/architecture/assistant-led-setup-contract.md` with observed findings, chosen package/storage/route shapes, request/review/version rules, exact create/reuse identity, consent and lifecycle state tables, lock/admission ordering, idempotency/reconciliation proof, reset inventory, and safe failure codes. Resolve the PRD's four implementation questions or identify a concrete blocked dependency. Update proposed Relevant Files/task bindings to the decision; do not leave two alternative implementations for the next agent to build.
  - [x] 1.7 Run the bounded characterization suites used by the spike. Record results and unknowns. Confirm that later groups have concrete service contracts and that any unsupported live helper blocks only affected proof, not unrelated read-only/unit work. No approval shortcut is an acceptable way to resolve a blocker.
  - [x] 1.8 Demo: capture the manual baseline/native-picker evidence and the chosen pre-HQ panel placement using `wt demo`; store sanitized screenshots under `tasks/screenshots/assistant-led-setup/`. Clearly label existing behavior versus any prototype; claim no delivered assisted setup yet.
  - [x] 1.9 Commit: "docs(setup): define assistant-led File Janitor execution contract"

- [x] 2.0 Deliver “Set up for me” through reviewed workspace preparation `Model: Sonnet 5`
  - [x] 2.1 Add focused service/HTTP/JS test fixtures for hired-without-HQ, active, proactively paused, missing/repair/unavailable identity, and zero/one/several owned File Janitor targets. Include forbidden-call spies for model calls, filesystem enumeration of unapproved roots, task starts, grants, monitoring, and file actions. Pin the read-only recommendation/proposal invariants before introducing writes.
  - [x] 2.2 Implement the canonical candidate resolver and read-only proposal projection selected in group 1. Hydrate capability/provenance/wizard data, include supported legacy identity, and distinguish no target from failed lookup. Prefer the exact active run; ready workspaces offer read-only existing status. Return a decision/manual-repair state for ambiguous/unsupported candidates rather than guessing or provisioning a duplicate. Group 4 completes adoption UX, but these safeguards must work now.
  - [x] 2.3 Build the concrete reviewed plan from the effective built-in File Janitor blueprint and current strict team plan. Disclose workspace create/reuse, configured curator/model state, metadata-only operation, upcoming folder/monitoring consent, schedule/timezone, and no automatic tasks/files. Bind its normalized revision to the owner, target, blueprint, and actual create/reuse decisions. Custom-agent collisions or changed effective plans require an explicit reviewed resolution; unknown template resolution fails closed.
  - [x] 2.4 Implement the bounded durable run/operation store selected in 1.6, including migrations only if needed, canonical references, distinct setup/health/first-result states, version checks, ownership, and uniqueness for one active user/target operation even with different client request keys. Persist acceptance/claim before consequences; include resume/deferral/retry/invalidation fields needed by later groups. Integrate new storage into reset inspection/apply policy at introduction, with upgrade/reset tests. Register work admission before any detached work and make shutdown join it. `Model: Opus 5`
  - [x] 2.5 Connect accepted proposals to the canonical reviewed creator/staffing path; do not call the permissive name/template-only helper as an authorization bypass. Carry operation identity into resource ownership/provenance so partially persisted profiles/workspaces can be reconciled without display-name guesses. Preserve root-agent/workspace-copy semantics, strict rollback of newly owned resources, reused-profile protection, capability snapshots, and current deletion/placement contracts. Stop at the folder-permission checkpoint; no setup/help/batch task, grant, scan, or watcher may start. `Model: Opus 5`
  - [x] 2.6 Expose only the owner-scoped typed proposal/status/accept/defer/resume operations settled in 1.6 through the existing server architecture. Enforce method, identity, version/review, target, and current allowed-action checks on the server; use sanitized failure categories. Wire the coordinator after its canonical dependencies, update route goldens/tests, and preserve ordinary Ask gating and Ori Guide's lack of mutation authority.
  - [x] 2.7 Add the real shared setup-card controller/template/styles with explicit target identity rather than page-global workspace inference. Render the actual Assistant identity and a proposal with **Set up for me**, details, manual customization, and **Not now**. Mount it outside the disabled pre-HQ composer path; keep it reachable across authenticated pages without adding a new navigation destination or always-visible Map row. Use real server data, not a simulated progress timer.
  - [x] 2.8 Route the existing File Janitor mission/deep link and non-modal Assistant recommendation to the same proposal/run. Respect existing hire locks, HQ briefings/overlays, durable recommendation deferral, and paused proactive behavior. Opening or refreshing must not allocate a run, open a picker, or start configuration; explicit revisit after deferral remains available. Before any target exists, manual customization opens only the existing blueprint-selected creator.
  - [x] 2.9 Render truthful creation/configuration receipts and next-permission state, refresh the existing map only after real creation, and preserve panel/focus/choices during updates. Support a reload returning to the same workspace checkpoint, explicit unavailable/stale state, and conditional labels such as **Configured; chat needs a model**. Do not mistake creator success or a persisted profile for capability readiness.
  - [x] 2.10 Verify proposal immutability and scope with wrong-owner, stale-revision, custom-profile collision, changed-blueprint/team, missing-source, root-agent storage, and no-model tests. Inject failures before/after profile creation, workspace persistence, and acknowledgment; prove repeated clicks/two tabs/new client keys do not duplicate resources. Assert read-only entry and stop-at-folder side-effect counters remain zero. Run affected coordinator/HTTP/creator/server/progression and card/module suites.
  - [x] 2.11 Demo: in `wt demo`, start from a real hired Assistant with no HQ/model, show the unchanged hire/HQ controls, accept one proposal, and reach the folder-permission checkpoint with one actual workspace/team. Reload and replay acceptance to prove the same run, then show Not now/manual-entry behavior from a fresh disposable fixture. Capture real UI and canonical resource evidence; later unimplemented steps must not be presented as completed.
  - [x] 2.12 Commit: "feat(setup): prepare File Janitor from a reviewed assistant proposal"

- [ ] 3.0 Deliver explicit permissions through the first useful review result `Model: Sonnet 5`
  - [x] 3.1 Reuse or extract only the File Janitor folder/automation presentation needed on Home, accepting an explicit workspace/run context. Preserve the existing console/wizard as consumers of the same domain contracts. Render the complete folder disclosure, including immediate-child metadata access, `Filed` creation, no content/model transfer, and separate file-operation approval; keep the suggested Downloads path unselected.
  - [x] 3.2 Wire the user-triggered native picker to the existing owner-scoped domain grant with `paused: true`, then reconcile the run/wizard from the result. The generic setup action schema must not accept arbitrary filesystem paths or fabricate picker selections. Reject stale/wrong-target grant continuation without overwriting a current root. Picker cancellation returns to the same checkpoint without an error/grant; no click replay auto-opens the picker.
  - [x] 3.3 Add permission-denial, unavailable-helper, folder-conflict, root/symlink-change, cross-owner, and lost-grant-response tests. Verify `Filed` is created only through the disclosed confirmed grant, watcher/schedule/assisted scan remain off afterward, and canonical access is preserved on safe same-folder reconciliation. Show the domain's authorized repair/other-folder route without deleting partial work or stealing claims.
  - [x] 3.4 Implement the separate monitoring/initial-scan review and commit, bound to the current root generation, privacy mode, actual schedule/timezone, run revision, and current automation state. Display watcher/debounce/settling details from effective configuration. Refresh stale disclosures; ask for an unresolved timezone; **Not now** leaves monitoring off. Preserve existing automation approval, require a distinct **Prepare first review** decision when adoption lacks scan authorization, and never unpause a user-paused workspace on a generic continuation. Revalidate at the consequence boundary, including races with pause/revoke/privacy changes. `Model: Opus 5`
  - [x] 3.5 Use `SetupAdapter`/domain automation approval and actual watcher/scheduler lifecycle to activate only the reviewed monitoring. Separate saved approval from healthy registration; failed registration keeps an actionable receipt and safe retry rather than claiming activity. Advance only non-permission/resolved wizard steps and readiness, with no generic auto-confirmation of a grant or automation decision. No duplicate watcher/scheduler is registered on replay.
  - [x] 3.6 Implement the bounded initial-scan operation/outcome proof settled in group 1 over the existing scanner and atomic scan state. Persist/reconcile success with a batch **and success with no eligible files**, retaining a bounded timestamp/reason/count acknowledgment without inventing an empty batch. Bind it to the accepted operation/root and privacy state. Do not confuse candidate deduplication or a preexisting latest batch with proof of this operation's execution. Unresolved outcomes stop for reconciliation instead of blind replay; verify crash-before/after-domain-write cases. `Model: Opus 5`
  - [x] 3.7 Join the approved initial scan with existing scan locking/coalescing, settling, and deduplication. Reuse a verified pending result where appropriate; do not double-notify or automatically initiate another scan merely because a page refreshes. Keep classification metadata-only even if a model exists; a concurrently changed content mode must block the assisted action, not call a provider or reset the user's setting. Retain ordinary manual/watcher scan behavior and model-free operation.
  - [x] 3.8 Reconcile wizard readiness through its normal completion hook and preserve `pa-tidy-downloads`'s existing once-only reward/help-task completion. Persist the separate first-result outcome; a scan failure after wizard readiness leaves the mission completed but the card visibly needing scan attention. No browser callback, generated message, or proposed result may record either success.
  - [x] 3.9 Render actual batch, no-new-eligible-files, settling/exclusion, and scan-failure outcomes with live counts/times and specific actions. Link **Review proposed filing** to the existing console; do not build a second reviewer or preselect candidates. Show real monitoring/privacy state and only say “nothing has moved” when supported by the run/domain evidence, accounting for separately approved changes in another tab. Keep the narrow-screen/keyboard flow usable throughout.
  - [x] 3.10 Add real-domain/HTTP integration and JS tests for the full three-consent path, no model/HQ, grant-without-monitoring, changed monitoring review, duplicate activation/scan, successful zero-candidate receipt, still-changing files, failed watcher/scan, and once-only mission completion. Use spies and temporary-folder snapshots to prove no content reads/provider calls/task starts/move-or-Trash decisions/approval tokens/file mutations occurred through setup. Run affected File Janitor/wizard/coordinator/server and frontend suites.
  - [ ] 3.11 Demo: use `wt demo` and a real native-selected disposable folder with settled files to reach the first review in the same card. Show all three consent moments, actual watcher/readiness/scan evidence, untouched existing files, and the unselected existing review surface. Repeat with an empty/settling fixture and an induced registration or scan failure; record screenshots and real outcomes, not mocked success.
    - Real-domain partial evidence: isolated demo on port 8943 reached a two-proposal first result with healthy watcher/scheduler, unchanged fixture files, and zero preselected review rows; see `assisted-first-review-real-domain.png` and `assisted-first-review-console-unselected.png`.
    - NOT RUN: foreground native picker success/cancellation/focus and live induced watcher/scan failure. The disposable folder was configured through the existing manual API only to exercise canonical adoption and the downstream real domains; it is not picker evidence.
  - [x] 3.12 Commit: "feat(setup): grant folder access and prepare the first File Janitor review"

- [ ] 4.0 Make existing setups, manual control, and interruptions safe `Model: Sonnet 5`
  - [x] 4.1 Complete the PRD's existing-state matrix using the resolver from group 2: active run, one supported partial, ready/paused, multiple owned candidates, supported legacy Downloads Janitor, missing/unsupported wizard, and unreadable state. Reuse stable capability/provenance identity and preserve recorded preferences, grants, candidates, and history. A capability installed on an unsupported custom workspace remains a manual/repair case, not an implicit migration.
  - [x] 4.2 Add the matching proposal/selection/resume UI and owner-scoped acceptance for adopting a partial workspace. A ready workspace only opens current status/review; it is never reprovisioned or scanned by opening the card. Ambiguity requires a user choice. Existing content-enabled/privacy-customized setups show actual settings and route to manual review rather than executing a falsely metadata-only plan or resetting configuration.
  - [x] 4.3 Implement coordinated manual takeover: defer new assisted steps, safely reconcile any in-flight action, and open the same workspace's wizard/settings. Before a workspace exists, keep the standard blueprint creator path. On explicit assisted resume, re-read domain facts and preserve manually completed steps; changed scope/consent demands a fresh review. Add two-tab tests proving the two controllers cannot overwrite each other's grants/settings or issue a duplicate configuration action.
  - [x] 4.4 Implement and explain distinct **Close**, **Finish later**, and **Resume setup** behavior across Home and other authenticated pages. Persist deferral; closing only hides presentation, while Finish later stops later setup steps after in-flight work settles. Disclose any already active monitoring and offer its separate pause action; do not silently revoke access, delete resources, or stop unrelated routines. Reopening/reloading reads the same saved state without unintended continuation from a GET.
  - [x] 4.5 Complete startup/explicit-resume reconciliation at every consequence boundary using the group 2/3 operation receipts. Cover database/file-store disagreements, partial profile/workspace provisioning, saved grant versus lost acknowledgment, approved but failed watcher, and scan result versus unfinished coordinator receipt. Resume only previously authorized work against current canonical state; stale/ambiguous ownership stops safely, with truthful partial results and no name-based recovery or new resources. `Model: Opus 5`
  - [x] 4.6 Add bounded retry scheduling with a maximum of two automatic retries after the initial repeat-safe attempt, persisted attempt counts, a deterministic/fake-clock test seam, and bounded backoff. Distinguish verified failure from unresolved consequence. Permission denial, picker cancellation, conflicts, unsupported state, changed ownership/scope, or missing identity never auto-retry. A deferred/invalidated run cannot be restarted by an old timer; explicit Retry is versioned and revalidated, not a bypass. `Model: Opus 5`
  - [x] 4.7 Finish lifecycle invalidation and regression coverage for manual monitoring pause, folder revoke/relink, changed root/ownership, capability removal, workspace archive/deletion, Assistant identity loss, shutdown, and reset. Reuse existing reviewed deletion/removal policies; the coordinator must not repair a user removal by recreating state. Exercise reset admission for detached work, drain/join, startup recovery, and any new reset-table inventory using disposable fixtures. No stale timer/run/receipt may regrant access or unpause monitoring. `Model: Opus 5`
  - [x] 4.8 Add operational exits through existing authorized controls: Open workspace, Pause monitoring, Manage access, History, and a bounded repair route. These operate on the selected workspace only and preserve existing confirmations. Keep setup-completed history separate from current health; stale/unavailable reads label cached facts and disable consequential controls. Error copy names the failed step, saved successes, and the concrete next action without exposing internal paths/tokens/provider errors.
  - [x] 4.9 Run the complete interruption/existing-state matrix with persistence-reopen tests, controlled concurrent tabs/actions, root-swap/wrong-owner attempts, hostile display text, retry exhaustion, and manual changes during polling. Add negative assertions that the coordinator never calls file approvals/apply/undo or a content provider. Confirm no real local data is used; run affected Go race tests where concurrency is introduced, targeted modules, and existing manual/legacy browser regressions.
  - [ ] 4.10 Demo: in `wt demo`, adopt a partial workspace, defer at a permission checkpoint, reload/navigate/restart the isolated server, and resume the same resources. Show manual takeover, a bounded retry/failure, paused/ready behavior, and access revocation without resurrection. Compare canonical IDs and activity receipts before/after; capture screenshots and record unsupported/unexercised cases honestly.
    - Real-domain partial evidence: isolated demo on port 8944 preserved the exact run/workspace IDs across server restart, kept a deferred folder checkpoint inert, adopted a folder configured manually while deferred, and resumed at the fresh monitoring review. See `deferred-monitoring-after-restart.png` and `resumed-manual-folder-after-restart.png`.
    - NOT RUN live: induced watcher/scan failure with timed automatic retry, and access revocation UI. Deterministic domain/store/race tests cover these paths; no mock screenshot is substituted for live evidence.
  - [x] 4.11 Commit: "feat(setup): resume assisted setup without overriding user control"

- [ ] 5.0 Validate the delivered experience and prepare approval-gated delivery `Model: Sonnet 5`
  - [x] 5.1 Implement/finish `tests/assistant-led-setup.spec.ts` around the actual entry routes, proposal acceptance, permission boundaries, real wiring, first-result handoff, and recovery. Map every AC-01–AC-20 to an automated assertion and/or named real demo with evidence; explicitly distinguish mocked UI error cases from real-domain/native-picker proof. Complete missing checks rather than declaring coverage from test file presence.
  - [ ] 5.2 Add `tests/assistant-led-setup.a11y.spec.ts` and focused view/focus tests for keyboard-only actions, accessible names, polite non-repeating announcements, nested-dialog/picker return, stable focused controls during refresh, long safe names/errors, narrow layout, reduced motion, and text-not-color-only state. Confirm Today/Ask/hire/HQ/help remain reachable and retain their existing permissions.
    - Automated accessibility coverage passes for keyboard order, labelled structure, live status, reduced motion, and 390px layout. NOT RUN: foreground native-picker focus restoration; mocks/helper termination are not substituted.
  - [x] 5.3 Perform a diff-focused security/recovery review of the actual implementation against the contract: ownership and CSRF/origin protections inherited from the app, stale approvals, strict creator reuse, raw-path boundaries, prompt/text injection, cloud/content exclusion, directory swaps, crash receipts, retry races, manual takeover, and removal/reset. Verify new or changed filesystem state uses repository permission conventions. Return bounded findings, fix them, and add regressions for each substantive issue. `Model: Opus 5`
  - [x] 5.4 Exercise existing File Janitor/legacy/manual wizard, team creation/root-agent storage, starter missions/rewards, hire/HQ, and file-review/undo regressions using the affected suites listed above. If setup code was shared, verify no other wizard gained automatic confirmation. On disposable files only, prove user-initiated file review still requires its exact separate approval and history remains correct.
  - [x] 5.5 Update `docs/architecture/assistant-led-setup-contract.md` and affected architecture/API/user-facing documentation to the final implemented routes, card states, permissions, resume/close/defer behavior, and unsupported environments. Replace resolved design alternatives with actual ownership and evidence. Do not rewrite historical PRD decisions or broaden runtime skills to authorize File Curator setup/file mutations. `Model: Haiku 4.5`
  - [ ] 5.6 Compare the real assisted path with group 1's manual baseline using the same fixture/environment. Record consent moments, required navigation, time to first useful result (separating user/OS/settling waits), deferral/recovery observations, and comprehension checks where actually exercised. Assert the default needs three consent moments and no app-page navigation before its result; do not invent production conversion or participant data. Update the acceptance evidence map with remaining limitations.
    - The guide records the three decisions, zero app-page navigation, and restart observations. NOT RUN: trustworthy end-to-end timing and participant comprehension; the automated duration excludes the native picker and is not product timing.
  - [x] 5.7 Run the final repository gates from the isolated feature checkout: `make test`, `make lint-new`, `gosec` scoped to the actual changed Go packages, and `make test-js`; also run affected Playwright/a11y suites and `npm run test:smoke`, plus applicable frontend ESLint/Prettier checks. Use `make lint-new`, not whole-tree `make lint`, and do not substitute `gosec ./...` baseline noise for a scoped check. Run full npm lint/format checks when applicable and identify pre-existing findings without changing unrelated files. Fix introduced failures and record the exact commands/results; local targeted tests are not the final gate.
    - Final results are recorded in the test guide. Smoke was 47/51; the same four unrelated failures reproduced against fresh `origin/dev`. Scoped gosec found 12 unchanged baseline findings and zero on changed hunks.
  - [x] 5.8 Permission sweep: review recurring command-approval friction from actual execution, prefer existing `wt demo`/test entry points, and move repeated compound commands into a stable checked-in script only when justified. Propose only stable narrowly scoped allowlist entries, or report a clean sweep. Never authorize a transient PID/path or broaden runtime folder/agent permissions.
  - [x] 5.9 Write manual test guide: `tasks/test-guide-assistant-led-setup.md`, reflecting implemented behavior. Include branch/isolated demo startup, exact mission/panel entry, required desktop picker environment, disposable fixtures, no-model/no-HQ setup, three consent actions, first review/empty outcomes, interruption/manual/permission/failure cases, expected results, and explicit non-goals. Link sanitized screenshots and identify NOT RUN environments. `Model: Haiku 4.5`
  - [ ] 5.10 Demo: build the final branch and drive the complete experience with `wt demo` from a fresh hire through a real first batch, then revisit a deferred/recovered setup and verify manual fallback. Capture final desktop/narrow/keyboard evidence, actual grants/watcher/scan receipts, and unchanged files before separate review. No enabled mock-backed product path may remain; native/live failures must be fixed or explicitly reported as blocking the promised supported path.
    - Real-domain first-batch and restart/manual-adoption demos are captured. NOT RUN: native picker selection/cancel/focus, live retry failure, and access-revocation UI; the manual folder API bridge is not claimed as picker proof.
  - [x] 5.11 Commit: "test(setup): verify and document assistant-led File Janitor onboarding"
  - [ ] 5.12 Open PR → squash-merge to dev: **stop immediately before `wt pr` / PR creation and obtain explicit approval**. Present the diff summary, all gate results, acceptance matrix, sanitized demo evidence, and remaining limitations. Only after approval, use `wt pr assistant-led-setup`, address review/CI, and squash-merge to `dev` under the canonical workflow. This checklist's planning approval is not PR permission.
  - [ ] 5.13 Run `wt done assistant-led-setup` after merge; archive the planning/test artifacts and clean up only the authorized feature worktree through the guarded workflow. Report what merged and what was actually tested; do not imply a release or production installation occurred.
